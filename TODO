
We have two package.json files in the project, one in the root directory and one in the client directory.
We also have two package-lock.json files in the project, one in the root directory and one in the node_modules directory.

Webscraper is not working correctly.
    -Writes unassigned to database file
    -Doesn't write all lines to database file at a time
    -Doesn't run when course already exists in file, but also doesn't return a ECTS value if it does

Need to impliment logout option/button somewhere on page(s)
